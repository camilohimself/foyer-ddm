---
import Layout from '~/layouts/PageLayout.astro';
import { getCollection } from 'astro:content';

import Hero from '~/components/widgets/Hero.astro';
import Features2 from '~/components/widgets/Features2.astro';
import Content from '~/components/widgets/Content.astro';
import Testimonials from '~/components/widgets/Testimonials.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';
import Stats from '~/components/widgets/Stats.astro';
import { getUpcomingEvents, getUpcomingEventCount, formatDateRange, currentYear } from '~/utils/events';

import fddmSculptureLumiere from '~/assets/images/fddm-sculpture-lumiere.jpg';
import fddmTableeTerrasse from '~/assets/images/fddm-tablee-terrasse.jpg';

const metadata = {
  title: 'Retraites spirituelles en Suisse — Foyer de Charité Dents-du-Midi, Bex',
  ignoreTitleTemplate: true,
  description: 'Retraites spirituelles en Suisse romande de 1 à 6 jours face aux Alpes. Silence, enseignements, accompagnement spirituel. Le seul Foyer de Charité en Suisse, à Bex (VD).',
};

// --- Prochaines retraites (dynamique depuis la collection events) ---
const upcomingEvents = (await getUpcomingEvents()).slice(0, 3);
const eventCount = await getUpcomingEventCount();

// --- Mapping type → badge + label + href ---
const typeInfo: Record<string, { bg: string; label: string; href: string }> = {
  '6-jours': { bg: 'bg-primary-600', label: '6 jours', href: '/retraites/6-jours' },
  'week-end': { bg: 'bg-primary-600', label: 'Week-end', href: '/retraites/week-ends' },
  couples: { bg: 'bg-rose-300', label: 'Couples', href: '/retraites/week-ends#en-couple' },
  familles: { bg: 'bg-teal-400/80', label: 'Familles', href: '/retraites/week-ends#en-famille' },
  journees: { bg: 'bg-accent-400', label: 'Journée', href: '/retraites/journees' },
  mariage: { bg: 'bg-violet-300', label: 'Mariage', href: '/retraites/preparation-mariage' },
};

// --- Témoignages (dynamique depuis la collection testimonials) ---
const allTestimonials = await getCollection('testimonial');
const shuffled = [...allTestimonials].sort(() => Math.random() - 0.5);
const selectedTestimonials = shuffled.slice(0, 6).map((t) => ({
  testimonial: t.data.quote,
  name: t.data.name,
  job: t.data.retreatType || 'Retraitant(e)',
}));
---

<Layout metadata={metadata}>
  <!-- Hero : Vue Dents-du-Midi + Accroche -->
  <Hero
    actions={[
      {
        variant: 'primary',
        text: "S'inscrire à une retraite",
        href: '/contact/inscription',
        icon: 'tabler:arrow-right',
      },
      {
        text: 'Découvrir le programme',
        href: '/programme',
      },
    ]}
  >
    <Fragment slot="title">
      Un lieu de silence et de ressourcement<br />
      <span class="text-accent-400">Foyer de Charité Dents-du-Midi</span>
    </Fragment>

    <Fragment slot="subtitle">
      Retraites spirituelles de 1 à 6 jours
    </Fragment>

    <Fragment slot="bg">
      <div class="absolute inset-0">
        <img
          src="/images/fddm-facade-ete.jpg"
          alt="Le Foyer de Charité Dents-du-Midi à Bex, bâtiment principal entouré de verdure"
          class="w-full h-full object-cover"
          loading="eager"
        />
        <div class="absolute inset-0 bg-primary-900/50"></div>
      </div>
    </Fragment>
  </Hero>

  <!-- 6 Packages en cartes -->
  <Features2
    title="Plusieurs chemins, un même accueil"
    subtitle="De la journée de découverte à la retraite de six jours, chacun trouve son pas."
    tagline="Nos retraites"
    classes={{ headline: { title: 'text-4xl md:text-5xl font-playball' } }}
    items={[
      {
        title: 'Journées de ressourcement',
        description: 'Maurice Zundel, La Fontaine, l\'art de l\'icône, le silence intérieur... Chaque journée explore un thème à travers deux conférences, un repas partagé et un temps de recueillement. Ouvert à tous.',
        icon: 'tabler:sun',
        callToAction: { text: 'En savoir plus', href: '/retraites/journees' },
      },
      {
        title: 'Week-ends',
        description: 'Une pause au cœur de votre quotidien ! Trois jours pour vivre une halte spirituelle — seul, en couple ou en famille. En lien avec le temps liturgique ou sur un thème proposé.',
        icon: 'tabler:calendar-event',
        callToAction: { text: 'En savoir plus', href: '/retraites/week-ends' },
      },
      {
        title: 'Six jours de silence',
        description: 'Le format historique des Foyers de Charité. Six jours pour avancer dans la foi, portés par les enseignements et le silence.',
        icon: 'tabler:sunset-2',
        callToAction: { text: 'En savoir plus', href: '/retraites/6-jours' },
      },
      {
        title: 'Préparation au mariage',
        description: 'Approfondir le sens de l\'engagement. Deux jours pour aborder les thèmes essentiels du mariage, accompagnés par des couples animateurs et un prêtre. Organisation : Diocèse de Sion.',
        icon: 'tabler:rings',
        callToAction: { text: 'En savoir plus', href: '/retraites/preparation-mariage' },
      },
    ]}
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-surface-alt"></div>
    </Fragment>
  </Features2>

  <!-- Prochaines dates — Spotlight 1+2 -->
  <section class="py-12 md:py-20">
    <div class="max-w-5xl mx-auto px-4 sm:px-6">
      <div class="text-center mb-10">
        <h2 class="text-4xl lg:text-5xl font-playball text-primary-600">Prochaines retraites</h2>
        <p class="text-muted mt-3 max-w-2xl mx-auto">Les portes du Foyer sont ouvertes. Voici les prochaines dates.</p>
      </div>

      {/* --- Event principal (spotlight) --- */}
      {upcomingEvents[0] && (() => {
        const main = upcomingEvents[0];
        const info = typeInfo[main.data.type] || { bg: 'bg-gray-500', label: main.data.type, href: '/retraites' };
        return (
          <a href={info.href} class="group block mb-5 p-6 md:p-8 rounded-2xl border-2 border-accent-200 bg-gradient-to-br from-white to-accent-50/30 hover:shadow-xl hover:border-accent-400 transition-all duration-300">
            <div class="flex items-center gap-3 mb-3">
              <span class={`text-xs font-semibold text-white px-3 py-1 rounded-full ${info.bg}`}>{info.label}</span>
              <span class="text-xs font-semibold uppercase tracking-wider text-accent-500">Prochaine retraite</span>
            </div>
            <div class="md:flex md:items-start md:gap-8">
              <div class="flex-1">
                <p class="text-sm font-semibold text-primary-600 mb-1">{formatDateRange(main.data.dateStart, main.data.dateEnd)}</p>
                <h3 class="text-xl md:text-2xl font-bold text-primary-600 group-hover:text-accent-500 transition-colors leading-snug mb-3">
                  {main.data.title}
                </h3>
                <p class="text-muted text-sm md:text-base leading-relaxed mb-4">{main.body}</p>
                <p class="text-sm text-muted">
                  {main.data.preacher}
                </p>
                <div class="mt-4">
                  <span class="inline-flex items-center gap-2 bg-accent-400 text-white font-semibold px-5 py-2.5 rounded-full group-hover:bg-accent-300 transition-colors text-sm">
                    Découvrir
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 group-hover:translate-x-0.5 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
                  </span>
                </div>
              </div>
              {/* Image spotlight — remplacer src par la vraie photo */}
              <div class="hidden md:block md:w-64 lg:w-80 shrink-0 mt-4 md:mt-0">
                {main.data.image ? (
                  <img
                    src={main.data.image}
                    alt={main.data.title}
                    class="w-full h-48 lg:h-56 object-cover rounded-xl"
                    loading="lazy"
                  />
                ) : (
                  <div class="w-full h-48 lg:h-56 rounded-xl bg-primary-100/50 flex items-center justify-center">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-12 h-12 text-primary-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                      <rect width="18" height="18" x="3" y="3" rx="2" ry="2"/>
                      <circle cx="9" cy="9" r="2"/>
                      <path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/>
                    </svg>
                  </div>
                )}
              </div>
            </div>
          </a>
        );
      })()}

      {/* --- Events secondaires (2 cartes) --- */}
      <div class="grid gap-4 sm:grid-cols-2">
        {upcomingEvents.slice(1).map((event) => {
          const info = typeInfo[event.data.type] || { bg: 'bg-gray-500', label: event.data.type, href: '/retraites' };
          return (
            <a href={info.href} class="group block p-5 rounded-xl border border-gray-200 bg-white hover:shadow-lg hover:border-accent-300 transition-all duration-200">
              <div class="flex items-center gap-2 mb-2">
                <span class={`text-xs font-semibold text-white px-2.5 py-0.5 rounded-full ${info.bg}`}>{info.label}</span>
                <span class="text-sm font-semibold text-primary-600">{formatDateRange(event.data.dateStart, event.data.dateEnd)}</span>
              </div>
              <h3 class="text-lg font-bold text-primary-600 group-hover:text-accent-500 transition-colors leading-snug mb-1">
                {event.data.title}
              </h3>
              <p class="text-sm text-muted">
                {event.data.preacher}
              </p>
              <span class="inline-flex items-center gap-1 mt-3 text-sm font-medium text-accent-500 group-hover:text-accent-400 transition-colors">
                Découvrir <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 group-hover:translate-x-0.5 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
              </span>
            </a>
          );
        })}
      </div>

      <div class="text-center mt-8">
        <a href="/programme" class="inline-flex items-center gap-2 text-primary-600 hover:text-accent-500 font-semibold transition-colors">
          Voir tout le programme {currentYear}
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Une retraite, c'est quoi ? -->
  <Content
    isReversed
    tagline="Découvrir"
    title="Qu'est-ce qu'une retraite ?"
    image={{
      src: fddmSculptureLumiere,
      alt: 'Sculpture en bois suspendue et son ombre projetée par la lumière de la fenêtre, chapelle du Foyer',
      width: 800,
      height: 450,
    }}
    items={[
      {
        title: 'Le silence',
        description: 'Se retirer du bruit du monde pour écouter ce qui se dit au fond de soi, face aux montagnes.',
      },
      {
        title: 'Les enseignements',
        description: 'Deux enseignements par jour, donnés par des intervenants qui partagent leur expérience et nourrissent la réflexion.',
      },
      {
        title: 'L\'accompagnement',
        description: 'Un entretien personnel si vous le souhaitez. L\'Eucharistie quotidienne. Un temps pour prier à votre rythme.',
      },
    ]}
    callToAction={{
      text: 'Découvrir nos retraites',
      href: '/retraites',
    }}
  >
    <Fragment slot="content">
      <h3 class="text-2xl font-bold tracking-tight sm:text-3xl mb-2 text-primary-600">
        Un lieu pour se retrouver
      </h3>
      La communauté accueille ceux qui éprouvent le besoin de s'arrêter, de réfléchir, de se ressourcer —
      pour redonner vigueur et élan à leur quotidien.
    </Fragment>

    <Fragment slot="bg">
      <div class="absolute inset-0 bg-surface-special"></div>
    </Fragment>
  </Content>

  <!-- Stats : Chiffres clés -->
  <Stats
    stats={[
      { title: 'ans d\'accueil', amount: '55+' },
      { title: `retraites en ${currentYear}`, amount: `${eventCount}` },
      { title: 'Foyers dans le monde', amount: '75' },
      { title: 'en Suisse', amount: 'Seul' },
    ]}
  />

  <!-- Le Foyer en bref -->
  <Content
    tagline="Le Foyer"
    title="Le seul Foyer de Charité en Suisse"
    image={{
      src: fddmTableeTerrasse,
      alt: 'Grande tablée en terrasse du Foyer, repas partagé avec vue sur les montagnes',
    }}
    items={[
      {
        title: 'Fondé en 1970',
        description: 'Plus de 55 ans d\'accueil au pied des Dents-du-Midi. Le seul Foyer de Charité en Suisse. <a href="/le-foyer/histoire" class="text-primary-600 hover:text-accent-500 underline underline-offset-2">Découvrir notre histoire</a>.',
      },
      {
        title: 'Un lieu habité',
        description: 'Sept hectares de prairie et de forêt, une chapelle face aux Alpes, et une communauté qui prie et qui accueille.',
      },
      {
        title: 'Tout est prévu',
        description: 'Vous êtes reçus en pension : chambre, repas préparés par la communauté, et la liberté de ne penser à rien d\'autre qu\'à l\'essentiel. <a href="/sejour" class="text-primary-600 hover:text-accent-500 underline underline-offset-2">Infos pratiques</a>.',
      },
    ]}
    callToAction={{
      text: 'En savoir plus sur le Foyer',
      href: '/le-foyer',
    }}
  >
    <Fragment slot="content">
      <h3 class="text-2xl font-bold tracking-tight sm:text-3xl mb-2 text-primary-600">
        Bex, aux portes du Valais
      </h3>
      Un écrin de verdure face aux montagnes des Dents-du-Midi, à quelques kilomètres des rives du lac Léman.
      Un lieu qui ouvre aux profondeurs et donne saveur à la vie.
    </Fragment>
  </Content>

  <!-- Témoignages (dynamique) -->
  <Testimonials
    title="Paroles de retraitants"
    subtitle="Ce qui se vit ici ne s'explique pas toujours. Parfois, ce sont ceux qui l'ont vécu qui en parlent le mieux."
    tagline="Témoignages"
    testimonials={selectedTestimonials}
    callToAction={{
      text: 'Tous les témoignages',
      href: '/temoignages',
    }}
  />

  <!-- CTA bannière -->
  <CallToAction
    isDark
    classes={{ headline: { title: 'text-4xl md:text-5xl font-playball tracking-tighter mb-4' } }}
    actions={[
      {
        variant: 'primary',
        text: 'S\'inscrire à une retraite',
        href: '/contact/inscription',
        icon: 'tabler:arrow-right',
      },
      {
        text: 'Découvrir le programme',
        href: '/programme',
      },
    ]}
  >
    <Fragment slot="title">
      Prenez le temps de <span class="text-accent-400">vous retrouver</span>
    </Fragment>

    <Fragment slot="subtitle">
      Le Foyer vous accueille pour une retraite de 1 à 6 jours, au pied des Alpes.<br />
      Aucune situation financière ne doit être un obstacle.
    </Fragment>

    <Fragment slot="bg">
      <div class="absolute inset-0">
        <img
          src="/images/fddm-retraitants-alpage-sommet.jpg"
          alt="Groupe de retraitants au sommet d'un alpage face aux Alpes"
          class="w-full h-full object-cover"
          loading="lazy"
        />
        <div class="absolute inset-0 bg-primary-900/60"></div>
      </div>
    </Fragment>
  </CallToAction>
</Layout>
